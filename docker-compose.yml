services:
  frontend:
    restart: unless-stopped
    build: ./frontend
    ports:
      - 80:80
  
  backend:
    restart: unless-stopped
    build: ./backend
    environment:
      - ORIGIN=http://52.62.25.240:3000
    ports:
      - 3000:3000
    volumes:
      - db:/api/storage
  
volumes:
  db:
    name: backend-db

